{
  "name": "NativeBridge",
  "version": "${FBSTRING_PLUGIN_VERSION}",
  "description": "The plugin bridge between javascript and C++ code.",

	"plugins": [
    { "path": "npNativeBridge.dll", "public": true }
  ]
}